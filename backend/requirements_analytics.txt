# Requirements supplémentaires pour le système Analytics
# À installer avec: pip install -r requirements_analytics.txt

# Géolocalisation IP
requests>=2.31.0

# Parsing User Agent
user-agents>=2.2.0
pyyaml>=6.0

# Optionnel : GeoIP2 pour géolocalisation locale (plus rapide)
# geoip2>=4.7.0
# Nécessite le téléchargement de la base de données MaxMind GeoLite2

# Optionnel : Celery pour tâches asynchrones (recommandé en production)
# celery>=5.3.0
# redis>=5.0.0

# Optionnel : Pour les graphiques côté serveur
# matplotlib>=3.8.0
# seaborn>=0.13.0
